# Generated by roxygen2: do not edit by hand
# NAMESPACE for palbociclibTDM Package
# Palbociclib Population PK/PD Simulation & TDM Cost-Effectiveness Analysis
# ==============================================================================

# ============================================================================
# CORE SIMULATION & PHARMACOKINETIC FUNCTIONS
# ============================================================================

# NOT EXPORTED AS FUNCTIONS - Scripts are standalone R files in inst/scripts/
# This package uses script-based analysis rather than function-based API
# All analysis workflows are self-contained in numbered scripts (01-08)

# ============================================================================
# EXAMPLE DATA EXPORTS
# ============================================================================

# Sample datasets included in package
exportData("paloma_trials")
exportData("pk_literature")
exportData("ae_incidence")
exportData("dosing_scenarios")
exportData("cost_components")
exportData("validation_cohort")

# ============================================================================
# VIGNETTES & DOCUMENTATION
# ============================================================================

# Main analysis workflow vignette
# vignette("palbociclibTDM-workflow", package = "palbociclibTDM")

# ============================================================================
# IMPORTS FROM DEPENDENCIES
# ============================================================================

# Core data manipulation
import(tidyverse)
importFrom(dplyr, %>%, select, mutate, group_by, summarise, 
           filter, arrange, left_join, bind_rows, if_else, case_when,
           slice, pull, distinct, n, as.data.frame)
importFrom(tidyr, pivot_longer, pivot_wider, gather, spread)
importFrom(data.table, data.table, setDT, as.data.table, `:=`)

# Visualization
import(ggplot2)
importFrom(scales, dollar_format, percent_format, comma_format,
           label_number, label_dollar, label_percent)

# Statistical functions
importFrom(stats, rnorm, lnorm, rbinom, rgamma, sd, median, mean, 
           quantile, IQR, var, cor, lm, predict, summary, confint)
importFrom(grDevices, rgb, adjustcolor)

# Utility functions
importFrom(base, format, paste0, sprintf, abs, sqrt, exp, log,
           round, floor, ceiling, seq, rep, sort, order, unique)

# ============================================================================
# PACKAGE METADATA
# ============================================================================

# roxygen2 configuration
if (getRversion() >= "2.15.1") {
  useDynLib(palbociclibTDM)
}
